export enum EDirectionSort {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum ETime {
  MILLISECONDS = 'Milliseconds',
  SECOND = 'Second',
  MINUTE = 'Minute',
  HOUR = 'Hour',
  DAY = 'Day',
  MONTH = 'Month',
}

export enum EGender {
  MALE = 'Male',
  FEMALE = 'Female',
  OTHER = 'Other',
}
